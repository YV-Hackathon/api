version: '3.8'

services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: cms_db
      POSTGRES_USER: cms_user
      POSTGRES_PASSWORD: cms_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://cms_user:cms_password@db:5432/cms_db
      SECRET_KEY: your-secret-key-here
    depends_on:
      - db
    volumes:
      - .:/app
    # Migrations now run automatically in the Dockerfile

volumes:
  postgres_data:
